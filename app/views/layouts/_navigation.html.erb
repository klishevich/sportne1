<%= link_to "Спортнэ", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Выйти', destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to 'Войти', new_user_session_path %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Мой профиль', edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to 'Администрирование', users_path %>
      </li>
    <% end %>
    <div class="button_class_menu">
      <%= link_to 'Добавить видео', new_user_video_path(current_user), class: "btn btn-primary" if current_user %>
    </div>
  <% else %>
    <li>
    <%= link_to 'Регистрация', new_user_registration_path %>
    </li>
  <% end %>
</ul>
