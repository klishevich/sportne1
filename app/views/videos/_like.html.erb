<%= form_for(current_user.relationships.build(video_id: @video.id), remote: true) do |f| %>
  <div><%= f.hidden_field :video_id %></div>
  <%= f.submit "Like", class: "btn btn-large btn-primary" %>
<% end %>